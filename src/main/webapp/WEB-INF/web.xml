<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>photogear.app</display-name>

    <!-- ======================================================
         NOTA: Si usas Tomcat 9 (javax.servlet.*), cambia la
         declaración del namespace a:
         xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         version="4.0"
         y ajusta los imports Java correspondientes.
    ====================================================== -->

    <!-- Cabeceras de seguridad (CSP, HSTS, anti-clickjacking…) en TODA respuesta -->
    <filter>
        <filter-name>SecurityHeadersFilter</filter-name>
        <filter-class>app.photogear.filter.SecurityHeadersFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>SecurityHeadersFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Filtro CORS (necesario para desarrollo con frontend separado) -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>app.photogear.filter.CorsFilter</filter-class>
        <init-param>
            <param-name>allowedOrigins</param-name>
            <!-- En producción: reemplaza * por el dominio real del frontend -->
            <param-value>*</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <!-- Filtro de autenticación JWT (protege los endpoints de equipo) -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>app.photogear.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/api/equipment</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/api/equipment/*</url-pattern>
    </filter-mapping>

    <!-- Servlet de autenticación (Google OAuth) -->
    <servlet>
        <servlet-name>AuthServlet</servlet-name>
        <servlet-class>app.photogear.servlet.AuthServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>AuthServlet</servlet-name>
        <url-pattern>/api/auth/*</url-pattern>
    </servlet-mapping>

    <!-- Servlet principal de equipo fotográfico -->
    <servlet>
        <servlet-name>EquipmentServlet</servlet-name>
        <servlet-class>app.photogear.servlet.EquipmentServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>EquipmentServlet</servlet-name>
        <url-pattern>/api/equipment</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>EquipmentServlet</servlet-name>
        <url-pattern>/api/equipment/*</url-pattern>
    </servlet-mapping>

    <!-- Archivos de bienvenida (frontend estático) -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
        <!-- Cookie de sesión endurecida (aunque la auth use JWT, no sesión) -->
        <cookie-config>
            <http-only>true</http-only>
            <secure>true</secure>
            <same-site>Strict</same-site>
        </cookie-config>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- Páginas de error genéricas: evitan que Tomcat exponga stack traces
         y la versión del servidor (information disclosure) -->
    <error-page>
        <error-code>404</error-code>
        <location>/error.html</location>
    </error-page>
    <error-page>
        <error-code>403</error-code>
        <location>/error.html</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error.html</location>
    </error-page>
    <error-page>
        <exception-type>java.lang.Throwable</exception-type>
        <location>/error.html</location>
    </error-page>
</web-app>
